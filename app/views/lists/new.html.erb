
<h1 class="ui olive center aligned header">Create A New List</h1>
<%= form_with(model: [@board, @list], local: true, class: "ui form") do |f| %>
  <div class="field">
    <%= f.text_field :name, autofocus: true, class: "middle input" %>
  </div>
 <div class="ui olive center aligned header">
 <%= f.submit nil, class: "ui olive button" %>
 </div>
<% end %>